seed: 42
data:
  data_dir: "data"
  num_workers: 0
model:
  num_classes: 7
  metadata_dim: 0
  pretrained: false      # skips the ImageNet weight download, just for a quick check
  dropout: 0.3
train:
  epochs: 1
  batch_size: 8
  lr: 0.0001
  weight_decay: 0.00001
output:
  checkpoint_dir: "checkpoints"