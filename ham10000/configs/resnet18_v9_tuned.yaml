data:
  augment: true
  data_dir: /kaggle/working/Trustworthy-AI-for-Dermatology-Imaging/ham10000/data
  img_size: 224
  num_workers: 2
logging:
  checkpoint_dir: ham10000/checkpoints/resnet18_v9_tuned
  experiment_name: resnet18_v9_tuned
  save_dir: experiments/resnet18_v9_tuned
loss:
  alpha_mode: effective_num
  class_counts:
  - 890
  - 5367
  - 412
  - 257
  - 891
  - 86
  - 114
  effective_num_beta: 0.999
  focal_gamma: 1.5
  name: weighted_focal
model:
  architecture: resnet18
  dropout: 0.35
  freeze_epochs: 0
  metadata_dim: 0
  num_classes: 7
  pretrained: true
output:
  checkpoint_dir: ham10000/checkpoints/resnet18_v9_tuned
seed: 42
train:
  batch_size: 32
  early_stopping_patience: 12
  ema_decay: 0.999
  epochs: 45
  gradient_accumulation: 1
  lr: 0.0001
  max_grad_norm: 1.0
  scheduler: cosine
  use_ema: false
  use_weighted_sampler: false
  weight_decay: 0.0002