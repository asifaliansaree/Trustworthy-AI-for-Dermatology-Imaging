data:
  data_dir: "ham10000/data"
  num_workers: 2

model:
  num_classes: 7
  metadata_dim: 0        
  pretrained: true
  dropout: 0.3
  metadata_dim: 18       

train:
  epochs: 20
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.00001
  seed: 42

output:
  checkpoint_dir: "checkpoints"

logging:
  save_dir: experiments/metadata_fusion